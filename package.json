{
  "name": "fabio-zoratti-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:watch": "watchexec --watch src --watch public --watch astro.config.mjs --watch package.json --watch tsconfig.json --exts astro,ts,mjs,json,css,js,html,png,jpg,jpeg,gif,svg,woff,woff2,ttf,eot,pdf -- npm run build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.8.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18"
  }
}
